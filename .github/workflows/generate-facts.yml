steps:
  - uses: actions/checkout@v3
  - uses: actions/setup-python@v4
    with:
      python-version: "3.10"
  - run: pip install -r requirements.txt
  - run: python -m scripts.generate_facts
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
