# Telegram Bot «Столицы мира — тренировка» (AGENTS.md)

Сделать Telegram-бота с вебхуком (**FastAPI**), реализующего три режима обучения/тренировки по соответствиям **страна ↔ столица**.

---

## ПРАВИЛА И РЕЖИМЫ

Бот помогает выучить и натренировать соответствия **страна ↔ столица** тремя комплементарными режимами. Во всех режимах пользователь предварительно может выбрать **континент** (Европа / Азия / Африка / Северная Америка / Южная Америка / Австралия и Океания / Весь мир) и **тип вопросов**:
- *Страна → столица*;
- *Столица → страна* — вопрос формулируется: «Столицей какой страны является **Парамарибо**?»;
- *Смешанный* (оба направления вперемешку).

### 1) «Флэш-карточки» (обучение, 1 игрок)

- Бот показывает карточку:
  - «🇹🇭 Таиланд → ?» (если *страна → столица*)
  - «Столицей какой страны является **Бангкок**?» (если *столица → страна*)
  - В смешанном режиме направление чередуется.
- Под карточкой — **четыре варианта ответа**, а также кнопки «Показать ответ», «Пропустить», «Завершить».
- После выбора варианта бот показывает тост: «✅ Верно» или «❌ Неверно. Правильный ответ: …» (для длинных сообщений используется `show_alert=True`).
- По завершении сессии бот выводит список «**неизученных**» (неверные ответы) и предлагает повторить их отдельно.

### 2) «Игра на время» (спринт, 1 игрок)

- Длительность по умолчанию **60 секунд** (можно 30/90).
- Серия быстрых вопросов (кнопки 4 варианта). Направление вопросов — смешанное.
- После каждого ответа бот мгновенно отвечает тостом «✅ Верно» или «❌ Неверно. Правильный ответ: …» (`show_alert=True` при длинном тексте).
- За каждый верный — +1 очко.
- Кнопка «Пропустить» допустима и даёт **0 очков**.  
- Финальный вывод: «⏱ Время вышло! Ваш результат: **14 правильных из 18**».

### 3) «Дуэт против Бота: столицы мира» (кооператив, 2 игрока)

- Запуск в групповом чате: **/coop_capitals**.  
  Первые двое, кто жмут «🙋 Участвовать», становятся **Игроком 1** и **Игроком 2**.
- Настройки: число раундов (5/10/15), сложность бота-соперника:
  - 🟢 Лёгкий ≈ 30% верных ответов;
  - 🟡 Средний ≈ 60%;
  - 🔴 Сложный ≈ 80%.
- Каждый раунд: один и тот же вопрос на двоих → Игрок 1 отвечает, затем Игрок 2 (кнопки 4 варианта).
  Верный ответ игрока → **+1 очко Команде**.
  Затем отвечает «бот-соперник» (верно/неверно по вероятности сложности) → **+1 очко Боту** при верном ответе.
- После каждого ответа живой игрок получает тост «✅ Верно» или «❌ Неверно. Правильный ответ: …» (`show_alert=True` при длинном тексте).
- Время на ход живого игрока — **не ограничено**.
- Между раундами вставляется пауза 2–3 секунды для читаемости.  
- Итог раунда (пример):
  ```
  Вопрос: Какая столица у Нигерии?
  Игрок 1 → Лагос ❌
  Игрок 2 → Абуджа ✅
  Бот-соперник → «Лагос» ❌
  Счёт: Команда 5 — Бот 3 (Раунд 6/10)
  ```
- Побеждает команда или бот по итоговому счёту.

---

## 🎯 Цель

Сделать Telegram-бота с вебхуком (**FastAPI**), который:

- предоставляет три режима: **Флэш-карточки**, **Игра на время**, **Дуэт против Бота**;
- поддерживает фильтрацию по континентам и направлениям вопросов (*страна → столица*, *столица → страна*, *смешанный*);
- хранит базовую статистику пользователя только **в оперативной памяти** (успехи в спринте, список «к повторению»);
- реализует кооперативный матч 2×Bot с контролем очередности, паузами между раундами и честной обработкой кликов;
- имеет тестовую админ-версию кооператива для отладки (см. далее).

---

## 🏗 Архитектура

**FastAPI**
- Обрабатывает `POST /webhook` (апдейты Telegram).
- Служебные маршруты:  
  `GET /healthz`, `GET /set_webhook`, `GET /reset_webhook`.

**python-telegram-bot v20+**
- Создание `Application` в жизненном цикле FastAPI.
- `job_queue` для таймеров (спринт, паузы между раундами).
- Апдейты доставляются через `application.process_update(update)`.

**Модель взаимодействия**
- «Hub-and-Spoke» гибрид:
  - Одиночные режимы (карточки, спринт) происходят в **личках** с ботом.
  - Кооперативный режим инициируется в **групповом чате** (регистрация), но ответы игроков идут на **кнопки** под «якорным» сообщением в том же чате. Бот — «хаб»: ведёт матч и счёт, показывает прогресс.

**Состояние**
- Одиночные сессии — в `user_data` (прогресс карточек, спринт).
- Кооперативные матчи — в `chat_data['sessions'][session_id]`:
  - участники, параметры матча (континент, тип вопросов, сложность), текущий раунд, чья очередь, счёт команды и бота, активные джобы таймеров.
- Для Render Free достаточно памяти процесса; для персистентности можно позже подключить БД.

**Требования к архитектуре**
- Явно указывать `allowed_updates=[]` на регистрации вебхука.
- При старте сервиса — проверять вебхук и при необходимости пере-регистрировать.
- Поддерживать периодический health-check вебхука.
- Не удалять вебхук при остановке — Telegram поднимет контейнер по новому апдейту.
- В обработчиках кооп-матча проверять `session_id` и `player_id` в `callback_data` (игнор третьих лиц).
- Для контроля UI «гасить» кнопки (редактирование сообщения) после валидного ответа.
- Логировать ключевые шаги в DEBUG (инициализация матча, генерация вопроса, подсчёт очков, паузы).

---

## 📦 Основные сущности

### DataSource (данные стран/столиц)
- Загружается вручную из `capitals.json`.  
- `countries_by_continent: dict[Continent, set[Country]]`
- `capital_by_country: dict[Country, Capital]`
- `country_by_capital: dict[Capital, Country]`
- Трансформация по запросу для мультиязычности.  
- Алиасы не используются.

### CardSession (флэш-карточки, на пользователя)
- `user_id`
- `continent_filter`
- `mode` (*country_to_capital | capital_to_country | mixed*)
- `queue` (итератор/список оставшихся)
- `unknown_set` (ID элементов с неверными ответами)
- `stats` (показы/знания)

### SprintSession (спринт, на пользователя)
- `user_id`
- `duration_sec` (30/60/90)
- `continent_filter`, `mode`
- `score`, `attempts`
- `job_id` (таймер окончания)
- `current_question` (для контроля двойных кликов)

### CoopMatch (кооператив, на чат/группу)
- `session_id`
- `players: list[PlayerRef]` (ровно двое), `turn_index` (0/1)
- `bot_difficulty` (0.3/0.6/0.8)
- `rounds_total`, `rounds_played`
- `score_team`, `score_bot`
- `continent_filter`, `mode`
- `anchor_message_id` (якорь с кнопками и табло)
- `active_job_ids` (паузы между раундами)
- `current_question` (+ варианты, правильный ответ)

### PlayerRef
- `user_id`
- `display_name`

---

## ▶️ Валидация и генерация вопросов

- Источник данных: `capitals.json`.  
  Содержит страны, столицы, разбивку по континентам.  
- Генерация вопроса:
  1) фильтрация по континенту,  
  2) выбор направления (если «mixed»),  
  3) выбор пары (правильный ответ),  
  4) подбор **3 корректных, но неправильных** вариантов из того же пула,  
  5) случайная перестановка.
- Для кооператива — один и тот же вопрос показывается обоим игрокам; бот-соперник «отвечает» затем по вероятности сложности (фиксированная вероятность).

---

## ▶️ Потоки режимов

### Флэш-карточки
1. Пользователь в личке выбирает параметры (континент, режим вопросов) и жмёт «Старт».
2. Бот показывает карточку с четырьмя вариантами ответа; доступны «Показать ответ» и «Пропустить».
3. Ведётся `unknown_set`; по завершении — отчёт и предложение повторить «неизученные».

### Игра на время
1. Пользователь выбирает длительность (по умолчанию 60 с), континент, режим вопросов.
2. Старт: запускается таймер через `job_queue`.
3. Серия вопросов с кнопками; счётчик верных/ошибок.
4. По истечении таймера — «⏱ Время вышло!» и итоговый результат.

### Дуэт против Бота
1. В группе `/coop_capitals` → регистрация двух игроков.
2. Настройка матч-параметров (раунды, сложность, фильтры).
3. Каждый раунд: вопрос → отвечает Игрок 1 → отвечает Игрок 2 → отвечает «бот-соперник».
4. Начисление очков, инкремент раунда, пауза 2–3 сек, обновление табло.
5. Завершение по числу раундов → объявление результата и кнопка «🔄 Сыграть снова».

---

## 📦 Тестовая версия игры (админ)

Добавить **тестовую админ-версию кооператива** (только для `ADMIN_ID`), запускаемую отдельной кнопкой/командой:
- Автоматически регистрирует второго игрока-бота как «дамми-участника».
- Дамми отвечает строго по **фиксированной вероятности** (например, 50%).
- Остальная логика идентична кооперативу.

---

## 📂 Структура проекта

```
.
├── app.py
├── requirements.txt
├── AGENTS.md
├── data/
│   └── capitals.json           # данные: страны, столицы, континенты
├── bot/
│   ├── handlers_cards.py       # флэш-карточки
│   ├── handlers_sprint.py      # игра на время
│   ├── handlers_coop.py        # кооператив 2×Bot
│   ├── keyboards.py
│   ├── questions.py            # генерация вопросов/вариантов
│   ├── state.py                # модели сессий/матчей
│   └── utils.py                # форматирование
```

---

## 📜 requirements.txt

```txt
python-telegram-bot>=20.6
fastapi>=0.110
uvicorn[standard]>=0.30
pydantic>=2.6
```

*(При необходимости: `orjson` для быстрой загрузки JSON, `python-dotenv` для .env.)*

---

## ⚡ Быстрый старт локально

1) Создать `.env` с переменными окружения (см. ниже).  
2) Установить зависимости:
```bash
pip install -r requirements.txt
```
3) Запустить сервер:
```bash
uvicorn app:app --host 0.0.0.0 --port 8000
```
4) Пробросить публичный HTTPS-URL (ngrok / cloudflared).  
5) Зарегистрировать вебхук:
```bash
GET /set_webhook
```

---

## 🚀 Деплой на Render (Free)

1) Создать **Web Service** и подключить GitHub-репозиторий.  
2) Настройки:
- **Build Command**
```bash
pip install -r requirements.txt
```
- **Start Command**
```bash
uvicorn app:app --host 0.0.0.0 --port $PORT
```
- **Env Vars**: см. ниже.
3) После деплоя открыть:
```text
https://<service>.onrender.com/set_webhook
```

---

## ⚙️ Переменные окружения

**Обязательные**
- `TELEGRAM_BOT_TOKEN` — токен от @BotFather  
- `PUBLIC_URL` — публичный HTTPS-URL сервиса  
- `WEBHOOK_SECRET` — длинная случайная строка  
- `WEBHOOK_PATH` — путь вебхука (по умолчанию `/webhook`)  

**Рекомендуемые**
- `ADMIN_ID` — Telegram ID администратора (для тестового режима и сервисных команд)  
- `DEFAULT_LOCALE` — `ru`  
- `LOG_LEVEL` — `INFO` / `DEBUG`

---

## 🔐 Замечания по безопасности и честности

- В кооперативе проверять, что нажатия приходят **только** от зарегистрированных двух игроков (`player_id` в `callback_data`).
- «Гасить» кнопки после валидного ответа (редактирование сообщения).
- Хранить `session_id` в `callback_data`, валидировать по `chat_id`.
- Ограничить команду запуска тестового режима по `ADMIN_ID`.
- Для стабильности: ловить `telegram.error.RetryAfter` и делать backoff при `429`.

## 💡 Общие требования к стабильности

- Вебхук (`telegram_webhook`) должен всегда возвращать **200 OK**; `Update.de_json` и `application.process_update` обёрнуты в `try/except`.
- Все вызовы `context.bot.*` (например, `send_message`, `edit_message_text`) выполняются в `try/except` (`telegram.error.TelegramError` или `httpx.HTTPError`) с логированием.
- Сервер предоставляет корневой маршрут `/` (или `/healthz`), отвечающий **200** для health-check.
- При старте приложения, если `get_webhook_info().last_error_message` не пустой, вызывается `delete_webhook(drop_pending_updates=True)` и затем повторная регистрация вебхука.

---

## 🧪 Диагностика и логирование

- Включить DEBUG-логи по ключевым точкам:
  - регистрация вебхука,
  - старт сессий/матчей,
  - генерация вопроса/вариантов,
  - начисление очков,
  - срабатывание пауз между раундами,
  - финальные итоги.
- Логи по пользователю: агрегация счёта в спринте, размер «unknown_set» в карточках.
